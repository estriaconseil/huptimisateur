/** Code PostgreSQL pour violation de clé étrangère (foreign key constraint). */
export const PG_FK_VIOLATION = "23503";

/** Nombre max de suggestions affichées dans le dialog d'optimisation. */
export const MAX_SUGGESTIONS = 20;

/** Seuil par défaut (heures) au-delà duquel une job bloque la journée entière. */
export const DEFAULT_FULL_DAY_THRESHOLD = 8;
